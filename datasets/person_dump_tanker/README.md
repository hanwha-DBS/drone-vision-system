# Dataset Template

Place your labeled training data here using the structure below:

```text
datasets/person_dump_tanker/
  images/
    train/
    val/
    test/
  labels/
    train/
    val/
    test/
```

This project expects YOLO-format labels using the class map from `configs/person_dump_tanker_dataset.yaml`.

