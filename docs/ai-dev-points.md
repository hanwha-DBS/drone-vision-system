# 드론 비전 시스템 — 개발 AI 포인트 (MBO 목표 시트)

> 본인 담당 범위: **AI 검출 엔진 + 관제 콘솔 UI/UX**. (드론/안전 관리 앱 개발은 타 담당자 범위로 본 시트에서 제외)
> 작성 기준: 실제 구현 상태 v0.9.0

---

## 목표 시트

**기간**
26.02.01 - 26.12.31

**지표**
점수

**시작 값**
0

**목표 값**
2.5

**가중치**
20%

**평가 방법**

1. 기능/비기능 요구사항 충족
   * 가중치: 필수 0.5 / 추가 0.2

   [필수] 무인 드론 AI 검출 엔진 자체 개발 — 5-Layer 다단계 검출·검증 스택(검출 → 타일링 → VLM 검증 → 시간일치 확정 → 학습데이터 수집), 레이어별 On/Off A/B 측정 구조
   [필수] 관제 콘솔 UI/UX 1차 구축 — 광산 관제 표준 UX 벤치마킹, 실시간 뷰어 HUD·위험도 신호등 4단계·KPI 지표 화면 설계 및 구현
   [필수] AI 검출 엔진 고도화 — 최신 검출 모델 스택 교체 및 앙상블 적용, 오탐 저감 검증 레이어 강화, 무인 자동 운영 파이프라인(자동 투입 → 검출 → 이벤트/학습데이터 자동 적재)

   [추가] 모델 파인튜닝 및 성능 목표 달성 — 현장 검증셋 기반 베이스라인 측정 후 자체 학습 검출기 투입(오탐 −30%, 사람 검출 Recall +10%p)
   [추가] 드론 실시간 스트림(RTMP) 연동 및 실시간 검출 — 드론/도크 라이브 스트림 직접 수신(1080p) → 프레임 샘플링 → 검출 파이프라인 실시간 투입. 녹화 영상 후분석에서 비행 중 실시간 감시로 확장
   [추가] 발파기 연동 — 검출 알람 ↔ 발파 인터록/현장 경보 연동

2. 일정 요구사항 충족
   * 가중치: 1.0
   : 26.12.31 기준 개발 및 현장 검증 준비 완료

**평가등급 부여 기준**
S : 2.9
A : 2.7
B+ : 2.5
B : 1.5
C : 0

**1분기 목표**
요구사항 정의
AI 모델 리서치 및 검출 스택 선정

**2분기 목표**
무인 드론 AI 검출 엔진 자체 개발
관제 콘솔 UI/UX 1차 구축

**3분기 목표**
AI 검출 엔진 고도화 (모델 스택 교체·앙상블 적용, 오탐 저감)
AI 학습 데이터 파이프라인 구축 (자동 라벨링 + 하드네거티브 수집)

**4분기 목표**
모델 파인튜닝 및 성능 목표 달성
드론 실시간 스트림(RTMP) 연동
발파기 연동
현장 검증 준비 완료

---

## 점수 산정

| 구분 | 항목 수 | 항목 가중치 | 소계 |
|------|---------|-------------|------|
| 필수 | 3 | 0.5 | 1.5 |
| 추가 | 3 | 0.2 | 0.6 |
| 일정 | - | 1.0 | 1.0 |
| **만점** | | | **3.1** |

- 필수 전부 + 일정 달성 = **2.5 (B+, 목표값)**
- 추가 1개 = 2.7 (A) / 2개 = 2.9 (S) / 3개 = 3.1
- 사내 등급표가 고정값(S 2.4 / A 2.2 / B+ 2 / B 1 / C 0)인 경우: 필수 가중치를 0.5 → 0.33으로 조정하면 필수 합계 1.0, 목표값 2로 기존 표를 그대로 사용 가능

---

## 항목별 구현 근거 (평가 방어용)

| 항목 | 상태 | 구현 근거 |
|------|------|-----------|
| [필수] AI 검출 엔진 자체 개발 | ✅ 완료 | `engine.py`(ZMQ 서버 + 4스레드 세션), `detection/pipeline.py`(5-Layer 오케스트레이터), L1 검출·L2 SAHI 타일링·L3 VLM 검증·L4 ByteTrack N-of-M·L5 Active Learning 전 레이어 동작. `configs/runtime_detector.json`으로 레이어별 On/Off A/B 측정 가능 |
| [필수] 관제 콘솔 UI/UX 1차 구축 | ✅ 완료 | `index.html` 다크 관제 콘솔 — KPI 카드 스트립, 위험도 4단계 신호등(clear/notice/warning/critical), 라이브 뷰어 HUD 오버레이, 이벤트 아카이브, 미션 바. 벤치마킹: Komatsu DISPATCH / Cat MineStar Detect / Hexagon Mining |
| [필수] AI 검출 엔진 고도화 | ✅ 완료 | 최신 모델 스택 교체 및 앙상블 적용(`detection/detector.py`, `sahi_runner.py`, `vlm_verifier.py`), 하드네거티브 프롬프트 기반 오탐 reject, 폴더 감시 자동 투입 → `results/events/` 이벤트 자동 저장 → `datasets/active_learning/manifest.csv` 학습데이터 자동 적재 |
| [추가] 모델 파인튜닝 및 성능 목표 | ⬜ 미착수 | 학습 스크립트만 준비(`training/finetune_detector.py`). 선행 필요: 검증셋 1,000+ 프레임 라벨링, mAP/Recall/FAR 측정 스크립트 |
| [추가] 드론 실시간 스트림(RTMP) 연동 | ⬜ 미착수 | 하드웨어 확보(Matrice 3D + Dock 2, 라이브 1080p). 현재 엔진은 영상 파일/폴더 감시 입력만 지원 → RTMP 수신 입력 경로 신규 구현 필요 |
| [추가] 발파기 연동 | ⬜ 미착수 | 현재 로컬 단독 구동, 외부 통보/연동 경로 없음(`docs/dev-tickets.md` TICKET-007) |

관련 문서: `docs/advancement-roadmap.md`(로드맵·KPI), `docs/jira-epics.md`(EPIC 현황), `docs/dev-tickets.md`(티켓·벤치마킹), `docs/model-strategy.md`(모델 스택)
